#include <civic.h>

void printVec(int[n] ints) {
	for (int i = 0, n) {
		printInt(ints[i]);
		printSpaces(1);
	}	
	printNewlines(1);
}

export int main() {
	int[3] ints = [ 3, 12, 42 ];
	printVec(ints);

	return 0;
}