#include <civic.h>

void scanVect(int[n] ints) {
    for (int i = 0, n) {
        ints[i] = scanInt();
    }
}

export int main() {
    int[3] ints;
    scanVect(ints);

    // TODO reuse printVec method
    for (int i = 0, 3) {
        printInt(ints[i]);
    }
    return 0;
}

