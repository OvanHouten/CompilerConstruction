#include <civic.h>

int factorial(int n) {
	if (n <= 1) {
		n = 1;
	} else {
		n = n * factorial(n - 1);
	}
	return n;
}

export int main() {

	printInt(factorial(scanInt()));
	printNewlines(1);

	return 0;
}