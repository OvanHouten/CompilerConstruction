#include <civic.h>

int gcd(int a, int b) {
	int gcd;
	if (a % b == 0) {
		gcd = b;
	} else {
		gcd = gcd(b, a % b);
	}
	return gcd;
}

export int main() {

	printInt(gcd(scanInt(), scanInt()));
	printNewlines(1);

	return 0;
}

