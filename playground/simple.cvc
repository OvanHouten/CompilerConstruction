#include <civic.h>
#include "civic.h"

export int main() {
	// Print a number
	printInt(neg(13));
	/*
	 * Multi line comment
	 * asd.
	 */
	printNewlines(3);
	return 0;
}

int neg(int i) {
	return -i;
}