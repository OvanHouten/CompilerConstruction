#include <civic.h>

int fac(int n) {
	// For now we ignore any negative number :-)
	if (n <= 1) {
		n = 1;
	} else {
		n = n * fac(n - 1);
	}
	return n;
}

export int main() {

	printInt(fac(scanInt()));
	printNewlines(1);

	return 0;
}